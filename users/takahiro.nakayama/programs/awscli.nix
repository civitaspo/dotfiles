{ inputs, config, lib, pkgs, ... }: {
  # awscli.enable = true;
  # awscli.credentials = {
  #   "default" = {
  #     "credential_process" = "${pkgs.pass}/bin/pass show aws";
  #   };
  # };
  # awscli.settings = {
  #   "default" = {
  #     region = "ap-northeast-1";
  #     output = "json";
  #   };
  # };
}
