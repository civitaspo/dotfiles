#!/usr/bin/env bash

if ! env | grep -q '=op:'; then
  echo "[INFO] There is no environment variables by 1Password." 1>&2
  exit 1
fi

ENV_KV="$(env | grep '=op:' | fzf)"
ENV_KEY="${ENV_KV%%=*}"
ENV_VALUE="${ENV_KV#*=}"
echo "export ${ENV_KEY}=\"\$(op read ${ENV_VALUE})\""
