#!/usr/bin/env bash
set -euo pipefail

bin="/opt/homebrew/bin/terminal-browser"
if [[ ! -x "${bin}" ]]; then
  printf 'terminal-browser is not installed. Run: mise run brew\n' >&2
  exit 1
fi
exec "${bin}" "$@"
