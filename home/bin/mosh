#!/usr/bin/env bash
set -euo pipefail

name="$(basename "$0")"
bin="/opt/homebrew/bin/${name}"
if [[ ! -x "${bin}" ]]; then
  printf '%s is not installed. Run: mise run brew\n' "${name}" >&2
  exit 1
fi
exec "${bin}" "$@"
